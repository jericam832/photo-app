<%- model_class = Image -%>
 <section class="jumbotron text-center" style="background: #fff; margin-bottom: 0;">
  <div class="container">
    <h1 class="jumbotron-heading">My Images</h1>
    <%#=t '.title', :default => model_class.model_name.human.pluralize.titleize %>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <p>
      <!--Add cover btn-->
      <%= link_to t('.new', :default => t('New Image')), new_image_path, :class => 'btn btn-primary my-2' %>
      <a href="#" class="btn btn-secondary my-2">Edit cover</a>
    </p>
  </div>
</section>
<div class="album py-5 bg-light">
  <div class="container">

    <div class="row">
      <% @images.each do |image| %>
      <div class="col-md-4">
          <div class="card mb-4 box-shadow">
            <%= link_to image_path(image)  do %>
               <%= image_tag image.picture.url, size: "225x225", class: "card-img-top" %>
            <% end %>
            
            <div class="card-body">
              <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to "View", image_path(image), class: "btn btn-xs btn-info" %>
                  <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_image_path(image), :class => 'btn btn-warning btn-xs' %>
                </div>
                <!--Created at timestamp-->
                <!--<small class="text-muted">9 mins</small>-->
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
